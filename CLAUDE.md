# Claude contributor note

Read and follow [AGENTS.md](AGENTS.md). The plan and product bar are in
[docs/REORG-PLAN.md](docs/REORG-PLAN.md).

Work comes from `python tools/fleet/work.py next`. Set `FLEET_AGENT` to
your agent name before starting. Use Windows-native commands, keep retail
bytes and raw evidence under ignored `workspace/`, stage explicit paths, and
let the pre-commit hook run.
