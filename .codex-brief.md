# Open-BFME Runtime Reds - Three Fixes Required

**Repo:** C:\Users\Jonathan\Desktop\open-bfme (Godot 4.7 remake of BFME2/RotWK 2.01)  
**Head:** d642b2f

These are three acknowledged regressions with existing watchdogs. Fix causes, never weaken thresholds. All thresholds are correct and must stay unchanged or stricter.

---

## RED 1: Fords Readiness Budget (10.4–11.3s vs 10s limit)

**Current:** Repeated startup measurements read 10.4–11.3 seconds against the 10-second limit.

**Root cause:** The threshold is correct; the startup is too slow. Time goes to content load, pack indexing, or scene instantiation.

**Your task:**
1. Find the runner/gate that measures Fords readiness (search `game/tests/` and tools for "Fords" or "readiness", also check for "10.0 second" or "10000 ms" thresholds)
2. Profile where the time goes: content load, pack indexing, scene instantiation
3. Fix the actual cost (do NOT raise the limit or silently defer content the match needs at tick 0; retail parity must hold)
4. Run the measurement at least 5 consecutive times and report all numbers
5. All must be under 10.0 seconds

---

## RED 2: Menu→Match→Menu Lifecycle (+7 Retained Objects in Cycle 2)

**Current:** After cycle 2 (menu→match→menu→match→menu), 7 Objects retained while nodes/resources/orphans return to baseline.

**Root cause:** Object leak in the lifecycle. Must identify which objects are retained by class/instance.

**Your task:**
1. Find the lifecycle runner that cycles menu→match→menu (search `game/tests/` for "lifecycle", "menu.*match", or "cycle")
2. Reproduce the +7 retained Objects after cycle 2 using Godot's object leak diagnostics (debug prints, instance counts by class)
3. Identify which exact object types leak (use `get_class()` or Godot's object counts)
4. Fix the retention at its source
5. Verify: retained-object delta is 0 across three full cycles (menu→match→menu×3), with nodes/resources/orphans baseline throughout
6. **Extend the runner to three full cycles** (currently unproven; watchdog is effective so this should pass) and make it a tracked check
7. Run and confirm all objects return to baseline after each full cycle

---

## RED 3: Intermittent Headless GLTF "Parameter t is null" in Bound-Prop Loading

**Current:** Intermittent "Parameter t is null" error in bound-prop loading at load time. Error is swallowed (silent fallback).

**Root cause:** Race condition or null-handling defect in bound-prop loading path.

**Your task:**
1. Find the loader/test that exhibits the error (search for "Parameter t is null", "bound.*prop", "GLTF" in `game/tests/`, `game/src/`, importer, or loader code)
2. Reproduce by running the affected loader/test in a tight loop (20–50 iterations) until it fires
3. Identify the race or null check that fails (inspect the bound-prop loading code path)
4. **Critical:** Per AGENTS.md rule 5 ("No silent fallbacks"), if a prop fails to load it MUST fail loudly. After your fix, it should not fail at all.
5. After fix: Run the loader/test in a loop for a convincing repeat count (50+ iterations with no errors)
6. Report how many loops ran clean

---

## Environment & Constraints

- **BFME2_INSTALL:** `C:\Users\Jonathan\Desktop\open-bfme\.private\retail-work\editions\rotwk\layered-install\layer-1-bfme2`
- **Python:** `C:\Users\Jonathan\Desktop\open-bfme\.private\retail-work\tools\cpython-3.12.13\python.exe`
  - PYTHONPATH: `C:\Users\Jonathan\Desktop\open-bfme\importer;C:\Users\Jonathan\Desktop\open-bfme\.private\retail-work\tools\python-3.12-env\Lib\site-packages`
- **Godot:** Resolve via `tools\resolve-godot.bat`; runners run as `<godot> --headless --path game --script res://tests/<runner>.gd`
- **Footprint:** boot/lifecycle/loader code and specific runners. Avoid importer descriptor emission and module_contracts.py registry.
- **Shared tree:** Six other lanes are editing this tree. Commit focused, cite runners when green.

---

## Definition of Done (Per Red)

For each red:
1. Show failing/red measurement first (cite current numbers)
2. Show causal fix with reasoning
3. Show same measurement green afterward with specific criteria:
   - **Fords:** Under 10.0s repeatedly, at least 5 consecutive runs
   - **Lifecycle:** Retained-object delta 0 across three cycles
   - **GLTF:** Loop clean for convincing repeat count (50+ iterations)
4. All thresholds unchanged or stricter
5. Report honestly anything you could not close with evidence trail

---

## House Rules

- Commit on the current worktree branch; NEVER push; NEVER merge to main; NEVER `git stash`
- Godot runs: env OPENBFME_CONTENT=C:\Users\Jonathan\Desktop\open-bfme\.private\content-packs; exe C:\Users\Jonathan\Downloads\godot47\Godot_v4.7-stable_win64_console.exe; redirect runner output to uniquely-named %TEMP% files and read the file
- Python: C:\Users\Jonathan\Desktop\open-bfme\.private\retail-work\tools\python-3.12.13\python.exe with PYTHONPATH pointing at importer/
- Retail INI oracle: .private\retail-work\editions\rotwk\cache\effective-assets
- No pack builds/publishes/selection changes
- Commit messages end: Co-Authored-By: Codex Sol <noreply@openai.com>
- An Opus adversarial review gates the merge — write reports for a hostile reviewer; every claim needs a rerunnable command
