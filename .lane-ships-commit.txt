feat(naval): fire ship weapons from the authored descriptor

The compiled ship descriptors carried a resolved combat block that nothing
consumed, so no ship had ever fired. PlayableUnitRuntimeAdapter now projects
that block into the structure weapon row the sim already fires, and the
refusals are as load-bearing as the admissions: only four of the thirteen
effective SHIP objects author a weapon the runtime can execute.

Admitted, on the numbers the RotWK catalog compiles: the two shore bombard
ships (range 1000, minimum 400, damage 500 SIEGE, 5000ms fire rate, an
authored projectile object and speed - weapon.ini:13848-13892 and
13994-14042) and EvilFireShip's range-5 contact ram. Refused by name:
ShipMissileRangefinder, which compiles a weaponId with neither damage nor
range (the corsairs and both battleships); ElvenFireShip's AttackRange = 0;
a projectile object with no authored speed; and the four troop transports,
which author no weapon at all. An unwritten DelayBetweenShots is carried as
delay_between_shots_source = sage-default rather than passed off as a rate
retail wrote.

ship_weapon_runtime_runner: 20/20, driving the sim's existing structure
weapon engine unmodified - launch on the authored band, impact tick from the
authored projectile speed, authored damage on impact, and the 50-tick fire
rate gating the second shell. test_ships 71/71,
playable_unit_runtime_consumer_runner 113/113,
ship_scenario_admission_runner 11/11 unchanged.

Known gap: no placement path spawns ships yet, so the projection is reached
by the runner and by whoever wires naval placement, not by a live match.

Co-Authored-By: Claude <noreply@anthropic.com>
