# Lane CASTLE — "castle maps like Minas Tirith are SUPER laggy on load and the walls don't work"

See _common.md. Evidence from the owner's v0.2.8 dist run (C:\Users\Jonathan\Desktop\open-bfme\dist\v0.2.8\run.log, 99,530 lines): 99,466 lines are `[RetailSliceSim] CASTLE_AI_REJECT team=N structure=farm site_source=generic-navigation-cell:... reason=unsupported-structure` — the castle AI site search in game\src\retail_slice\retail_slice_sim.gd (`_try_castle_ai_site` ~line 31947 and its caller's generic-navigation-cell scan) iterates every navigation cell for a structure kind that is rejected with `unsupported-structure` before any site geometry matters, dry-running a construct and printing for each cell. That is the load lag. Also logged: 14x `CASTLE_WALL_DEFENSE_STALE type=MinisWallAUpgrade reason=missing-playable-structure-document`, `castle_gameplay_gaps=walkable-walls, defendable-gates, wall-mounted-defenses`, and `RETAIL_MAP_DATA_WALK_SURFACE_GAPS` for Minas ramps. The dist maps pack is abc27325…; the selected workspace maps pack is 6f6be4db… (newer); the Q51/Q56f/Q62 walkable-wall repairs exist only as LOCAL scratch cooks (orchestration\reports\report-q56f.md, report-hd-walls.md, report-minas-lag.md; queue rows Q51/Q56/Q62/Q64b).

Tasks:
A. Lag. (1) Failing-first test in the castle AI runner: the Minas site search issues at most O(candidates) dry-runs and zero per-cell prints for a structure kind the faction cannot build (assert a dry-run receipt counter / print count). (2) Fix: evaluate `unsupported-structure` (and any other kind-level refusal) ONCE per structure kind per team and skip the cell scan; demote the per-cell print to one summary line per (team, kind); bound the generic cell scan (ring around home, budget per tick) the way retail's skirmish AI uses base-layout sites plus a small search radius — cite the retail AI INI (aidata.ini / skirmish AI files) for the radius you choose. (3) Also check Q62(b): Minas map_data init 2368ms→9005ms from the per-ramp full-grid scan — fix it if it is in the same code path. Measure wall-clock of the Minas live boot and the 4,000-tick castle AI run before/after (workspace\logs\castle-owner\).
B. Walls. Determine exactly what "walls don't work" means today on the SELECTED pack 6f6be4db with current main: can units walk on walls? are wall-mounted defenses (MinisWallAUpgrade) buildable? do gates open/close? can units path through the gate? Run castle_wall_walk_runner, castle_gate_runner, castle_map_live_boot_runner on Minas Tirith and Helm's Deep and list each capability as WORKS / BROKEN with the log line. For each BROKEN item that is a CODE gap, fix it with a failing-first test. For each CONTENT gap (needs the maps-pack republish with the Q56f/Q62 repaired cook, or a missing playable-structure document for MinisWallAUpgrade in the men pack), write the exact republish/cook command(s) and the expected runner numbers — do NOT run publish/selection yourself; name it as the owner's next step. For `missing-playable-structure-document` on MinisWallAUpgrade: find the retail object (ministirithbuildings.ini) and why the faction cook skipped it.

Gates: castle AI runner (baseline 104/1, Minas red = Q51), castle wall walk (22/0), castle gate (47/0), live boot (8/0), state pin b025d162 and pathing pin 2e5ad580 unchanged.
