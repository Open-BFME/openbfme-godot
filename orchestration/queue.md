# Open-BFME live work queue

Seeded 2026-08-17 by the stage-2a triage lane
(`orchestration/briefs/stage2-triage-kimi.md`). This is the single live queue:
nothing cooked-but-unshipped or claimed-but-idle is invisible.

## How to use this file

- **Claim before you start.** Every lane writes its name into the `owner`
  column of a row before touching that work. An `unassigned` row is fair game;
  a claimed row is not.
- **Update on finish.** When the work lands, change the row's status and cite
  the commit/pack/gate output that proves it. A row is closed by evidence, not
  by assertion — delete it only once the evidence is in the row's history or a
  linked report.
- **Statuses:** `READY` (unblocked, can start now), `BLOCKED` (named
  prerequisite), `DECISION` (owner ruling required before any lane acts).
- **New work enters here first.** If it is cooked-but-unshipped or
  claimed-but-idle and it is not in this table, it is invisible — add the row.

## Queue

| id | item | evidence | status | owner |
|----|------|----------|--------|-------|
| Q1 | 79 missing-physical batch packs cooked, never activated (`{bfme2,rotwk}-missing-physical-20260816-batch-*`, 38,507 converted files); apply script `phase2-apply-selection-transaction-20260816.ps1` generated but never run | CLOSED 2026-08-17: one `apply-selection-transaction` (active men `4f92c8a4…`, angmar `48b89cf7…`, 99 supplements including all 79 batches); both roots `verified=true` `swaps=2`; `check_pack_addresses.py` PASS packs=200 roots=2; selection sha `04229f76…`. Report `orchestration/reports/q1-q2-pack-activation.md` | CLOSED | grok-q1q2 |
| Q2 | Men/Angmar vslice packs PUBLICATION_READY, selection never swapped (men bundle `4f92c8a4...`, angmar bundle `48b89cf7...`; live selection still men `3be646b0...`, angmar `662cf457...`) | CLOSED 2026-08-17: published existing PUBLICATION_READY trees via `publish_to_godot(select=False)` to both roots; digests match kimi (`4f92c8a4…` / `48b89cf7…`); `auditValid true` `conversionFailures 0`; selection untouched until the Q1 transaction. Report `orchestration/reports/q1-q2-pack-activation.md` | CLOSED | grok-q1q2 |
| Q3 | EVA recompose: 6 of 7 live eva-overlay packs lack `semanticFieldCoverage` (stale vs merged runtime); only `rotwk-angmar-eva-overlay` carries it | Q14 owner ruling 2026-08-18: EVA overlays kept at prior digests; recompose deferred to v0.2.5. | READY | unassigned |
| Q4 | Men faction republish (flaming trebuchet rock + WellHealFX particle); recook must emit the moduleContracts kinds or presentation work stays unbound on shipped content | CLOSED 2026-08-18 via Q14 r4 men pack `a0fde4ac…` selected as active. | CLOSED | grok-q14-finish |
| Q5 | `retail_state_pin` drift — DIAGNOSED, no re-mint needed. Sole cause: `3c67c7c` wrote a derived memo key `structure_module_contracts_attempted` into hashed structure rows (classification (c)); already fixed by `3dbbc9f`, which restores `0e4bcdbf…` exactly. Hash green at HEAD `e1ba04c` (3 runs, with and without content). Residual: the gate STEP still fails on `$forbiddenDiagnostics` (4 `ERROR:` structure-armor lines from `819b3e3`) — see Q11. | `orchestration/reports/q5-state-pin-drift.md`; `workspace/logs/q5-sweep-results.txt`; `workspace/logs/q5-3dbbc9f-memo-readded-statepin.txt`; `workspace/logs/q5-gate-emulation.txt` | CLOSED (diagnosis) | opus-q5 |
| Q11 | `retail_state_pin` GATE step red for a non-hash reason: the frozen fixture has no compiled armor for `barracks`/`farm`/`archery_range`/`stable` (only `fortress` in `DEFAULT_STRUCTURE_ARMOR`), so `819b3e3`'s guard emits 4 `ERROR:` lines and `Invoke-ProofChecked`'s `ERROR` forbidden regex fails the step. Fix the fixture floor or the log level — NOT the regex. | `orchestration/reports/q5-state-pin-drift.md` (Gate emulation); `workspace/logs/q5-gate-emulation.txt`; `tools/proof-gate-common.ps1:82`; `tools/gate-retail.ps1:520` | READY | unassigned |
| Q12 | `retail_scripted_state_pin` red and only partly explained: gate ratchet `0ae2055f…` (set `c5eb584`), HEAD measures `b0a3b163…`. The memo hunk accounts for `5ec18b4e ↔ b0a3b163` only; the `0ae2055f → b0a3b163` step is authored behaviour inside the `3c67c7c` mega-commit and is UNDIAGNOSED. Needs hunk-level reverts on the scripted fixture. | `orchestration/reports/q5-state-pin-drift.md`; `workspace/logs/q5-scripted-sweep.txt`; `tools/gate-retail.ps1:244-245` | READY | unassigned |
| Q13 | Ordinary-weapon projectile flight time and retail warhead-nugget radius damage; remove invented hero cleave. Implementation through `fabb94e`; verifier FIX-FIRST on F1–F4. Fixes landed `d6e3acb` / `f000569` (provenance + valueSemantic, truthful damageTypeSemantic, weapon-mode damage_components persist, focused-gate marker/comment, Q15/Q16). Awaiting re-verify. | `orchestration/reports/q13-fixes.md`; brief `orchestration/briefs/q13-fixes-grok.md`; verifier `orchestration/reports/q13-verify.md` | CLOSED (verify r2 ACCEPT 2026-08-17) | grok-q13-fixes |
| Q14 | Recook, publish, and select affected faction packs so Q13's compiled `projectileSpeed`, projectile id, `radiusDamageAffects`, and radius/taper component rows reach shipped content; publish the resulting versioned `dist/v<version>/` build. Absorbs Q3 (EVA recompose) and Q4 (Men republish). | CLOSED 2026-08-18: 7 new faction digests selected (active men `a0fde4ac…`); selection sha `4e3e7024…`; `check_pack_addresses` PASS packs=200 roots=2; checkpoint no STOP; dist `dist\v0.2.4\` zip sha256 `cccacf03…`. EVA/maps deferred (Q3 → v0.2.5). Report `orchestration/reports/q14-finish-v024.md`. Commits `fdbb648` / `6a537f2` / `b51be4d`. | CLOSED (v0.2.4 alpha shipped + verified ACCEPT 2026-08-18) | grok-q14-finish |
| Q15 | `tools/gate-m2-focused.ps1` dead since Q1: `tools/m2-oracle-common.ps1:36` requires activePack `^bfme2-men-vslice/`; selection is now `rotwk-men-vslice` — decide: retarget the gate to the RotWK Men pack or retire it | `orchestration/reports/q13-verify.md` Part A item 3; `tools/m2-oracle-common.ps1:36`; `workspace/content-packs/selection.json` | DECISION | unassigned |
| Q16 | `retail_state_pin` fixture is synthetic and fires no ranged weapon; pin does not cover projectile combat — extend fixture (conscious re-mint) or add a second projectile-covering pin | `orchestration/reports/q13-verify.md` "The state-pin statement"; `game/tests/retail_state_pin_runner.gd:296-325` | READY | unassigned |
| Q6 | 6 pre-existing importer failures, red since before the 2026-08-16 rename: `test_playable_unit_import`, `test_rotwk_official_map_corpus`, `test_scripts_converter`, `test_special_disguise_prerequisite`, `test_w3d_chunk_backlog` (x2) | `orchestration/reports/stage1-verify.md` round 3 criterion 2 / round 4 caveat 3 | READY | unassigned |
| Q7 | 15 triaged runner regressions with named causal commits (drawable/FXEvent refactor `7874318`, frame-clock `a733ffc`/`20f6c9c`, weapon-cycle `629b72f`, ParticleSysBone `ba1b701`, Men profile `5120925`, naval `b4f88c1`); owners never re-tested | `orchestration/reports/failure-triage-table.md` (15 verdict rows, all REGRESSION) | READY | unassigned |
| Q8 | Castle lanes L3-L10 briefs written, never executed (L1/L2a/L2b have reports; L3-L10 are brief-only) | `workspace/orchestration/fable-wave/castle-lanes/brief-castle-L3.md` … `brief-castle-L10.md` | READY | unassigned |
| Q9 | men-fords generated profile regeneration — re-mint `virtualPaths`; the 3 labels inside `men-fords-v0-complete.generated.json` still read `.private/retail-work`, a directory that no longer exists (labels are stale; the pinned identity `0bc2e767...` must be preserved by regenerating through the importer, not hand-editing) | `orchestration/reports/stage1-verify.md` round 4 caveat 2 | READY | unassigned |
| Q10 | Stage-1 intermediate commits `9c19da5`/`72a5665` left 80 MB of briefly-tracked `game/.private/` blobs (own-runtime WotR screenshots, runner logs, one agent transcript) in public history; untracked at HEAD. Owner ruled 2026-08-17: leave history as-is, no filter-repo. Recorded, closed. | `orchestration/reports/stage1-verify.md` round 4 caveat 4; `git log --diff-filter=A 9c19da5 72a5665 -- game/.private` | CLOSED (owner decision) | owner |
| Q17 | Q13 non-blocking follow-ups (verify r1/r2): splash lands at launch-time target position while direct hit homes; target dying mid-flight cancels all splash; structure-splash and ALLIES/NEUTRALS paths untested; `damage.equivalentSources` dropped for multi-nugget weapons (info preserved in components); archer presenter still release-token driven (no double arrows). | `orchestration/reports/q13-verify.md` | READY | unassigned |
| Q26 | Admit retail's untriggered SubObjectsUpgrade (elves NoldorWarrior / NoldorWarriorHorde convert regression: commented TriggeredBy raises instead of becoming an inert row). No pack builds in this lane. | CLOSED 2026-08-18: `compile_sub_objects_upgrades` emits `untriggered: true` / `reason: no-active-triggeredby-authored` when TriggeredBy is absent. Elves convert `converterGapCount` 2→0; NoldorWarrior + NoldorWarriorHorde converted. Full importer suite 6 failed / 3742 passed / 0 errors (exact Q6 names). Report `orchestration/reports/q26-subobjectsupgrade-untriggered.md`. | CLOSED | grok-q26 |
| Q27 | typed-module over-strictness cluster — contracts that raise on retail-authored-but-inert shapes (SubObjectsUpgrade done; audit the other 6 requires-TriggeredBy contracts + men GondorKnightsofDol AutoHealBehavior healonlyifnotunderattack) — one lane to admit them as deferred rows. | Q26 closed the SubObjectsUpgrade case. Sibling raises remain: AttributeModifierUpgrade :498, GeometryUpgrade :593, ModelConditionUpgrade :994, BuildableHeroListUpgrade :1617, AllowBannerSpawnUpgrade :1641, ObjectCreationUpgrade :6334. Brief `orchestration/briefs/q26-subobjectsupgrade-untriggered-grok.md` DoD 4. | READY | unassigned |
| Q28 | Q13 splash hits ONE member per battalion with no spill-over (playtest finding) — a rock on a horde should damage several members; verify retail semantics (per-member radius test vs per-battalion) and fix `_apply_radius_damage` accordingly | `orchestration/reports/playtest-v024-trebuchet.md`; `game/tests/playtest_trebuchet_splash_runner.gd` | READY | unassigned |
| Q29 | Boot error 3x per slice boot: `retail_structure.gd:721` reads `facts["construction"]` behind `if not construction_omitted` when the key can be absent; traces to `34e6cfa` | `orchestration/reports/playtest-v024-trebuchet.md` | READY | unassigned |
| Q30 | `retail_ai_ladder_runner` 13/4 on HEAD (ladder_medium_beats_easy, hard_beats_medium, footprint_* ) — deterministic, unattributed, no recorded baseline; bisect in an out-of-tree worktree (needs class cache) and record a baseline | `orchestration/reports/playtest-v024-trebuchet.md` | READY | unassigned |
| Q31 | v0.2.4.1 hotfix: `buildingLifecycle construction facts and phase animation disagree` blocks skirmish start and the Fords scenario (Q29 subsumed) | claimed 2026-08-18; brief `orchestration/briefs/hotfix-v0241-lifecycle-construction-grok.md` | READY | grok-hotfix-0241 |
| Q33 | Tree sway is an invented idle breeze (game/src/retail_slice/retail_tree_sway.gd: 4°/45f/0.35 applied by name-match) — retail drives W3DTreeDraw trees from the MAP's world-info sway settings + `SET_TREE_SWAY` script action, wind direction `gamedata.ini DownwindAngle=-0.785`, and per-model authored idle anims. Lane: parse the map sway world-info (importer sage_map / map_profile → map doc), bind DownwindAngle, retire the idle-breeze constants (unauthored = still, not invented), honour authored W3D idle anims for trees that carry them; owner playtest note 2026-08-18 'trees don't naturally sway like RotWK, weird looking'. Presentation lane, after Loco A. | `game/src/retail_slice/retail_tree_sway.gd`; `naturetrees.ini` W3DTreeDraw; gamedata.ini:8583 | READY | unassigned |
